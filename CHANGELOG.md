# 更新记录

本项目从 1.0.0 开始公开维护。版本号遵循 `主版本.次版本.修订号`。

## 未发布

## 1.10.2

- 修复分类标签小字号文字发虚：分类标签与原地改名编辑框的 12px 文本改为按像素对齐渲染（`TextFormattingMode=Display` + ClearType），字形不再落在亚像素位置上发糊；改动仅作用于标签与编辑框样式，字号、字重、配色与命中区域不变。真实渲染前后对比取证：差异恰好落在分类标签区域（其余像素零变化）。

## 1.10.1

- 缩略图改为异步解码,消除大图首次显示的界面卡顿(现场观察 B-005 相关):卡片缩略图此前在 UI 线程同步解码,切换到含大量未缓存大图的分类时会阻塞界面。现在新增 `AsyncThumbnailImage` 卡片控件——缩略图缓存命中时同步显示已解码结果(零解码开销);未命中时先显示占位,在与既有 `ImageThumbnailConverter` 完全共享的缓存上后台解码,完成后回到 UI 线程设置冻结位图,快速切换绑定路径时只应用最新结果,解码失败或文件缺失保持占位不报错。缓存语义(LRU、64 条/32MiB 上限、文件变化失效、并发安全)与既有 20 项锁定测试完全一致,转换器仅新增只读缓存查询接口。首次显示新采集图片时缩略图会有约一帧的占位后即刻补上,属预期行为。

## 1.10.0

- 新增全局快捷键唤起：任何界面按 `Ctrl + Alt + V` 展开悬浮面板并定位到当前默认接收分类，已展开或拖放进行中时保持现状不打断。设置窗口在动效与开机自启之间新增"全局快捷键"区块，开关默认关闭，仅用户显式开启时才注册系统热键；组合键被其他软件占用时提示"全局快捷键注册失败，可能被其他软件占用。"并回退为关闭，不写入偏好。偏好以 `preferences.json` 原子持久化（旧版本文件缺字段回落默认关闭），启动按偏好自动注册（启动失败仅状态条提示、不改写偏好，下次启动自动重试），退出注销热键。Windows 实现，Mac 端共享偏好模型新增字段、界面接入待 Mac 开发恢复。

## 1.9.1

- 修复微信等来源一次复制图片偶发生成两张卡片（现场观察 B-006）：来源软件一次复制可能分多步写剪贴板，触发多个不同序号的通知，原有按剪贴板序号的去重拦不住。现在自动采集新增连续重复采集抑制——与最近一条成功采集完全相同的单条内容，5 秒窗口内不再重复入库：文本按整理插件处理后的全文精确匹配，图片按规范化后的像素指纹比对（位图与文件路径等不同表示之间可互认），抑制时状态栏提示"与最近收集的内容相同，未重复收集。"。仅比对最近一条成功采集，批量多条导入与外部拖入不去重，窗口外再次复制相同内容照常收集；保存失败不记录指纹，重新复制可再入库。Mac 端共享核心已具备该能力，采集接线待 Mac 开发恢复后启用。

## 1.9.0

- 新增插件系统首版：声明式文本整理插件。安装目录 `plugins/` 内建插件，数据目录 `plugins/` 放用户插件，每个插件一个 `plugin.json` 清单（id/名称/版本/kind=text-cleaner/有序正则规则），启用状态原子持久化到数据目录 `plugins-state.json`。插件默认全部关闭，在设置窗口新增"插件"区块逐个启用（显示名称、版本、描述或加载错误原因与插件目录入口），切换立即生效并自动保存；用户目录同名插件覆盖内建版本，清单非法的插件列出原因但不可启用。启用后规则在文字入库前按序应用（自动采集与外部拖入共用同一管线），正则以 CultureInvariant + Multiline 编译（行锚点按行生效），任何规则执行失败或结果被清成空白时回退原文——插件只允许"整理"内容，不允许吃掉内容。首版内建示例插件"整理空白"（统一换行为 CRLF、去行尾空白、压缩连续空行）。Mac 端共享清单解析与状态核心，界面接入待 Mac 开发恢复。
- 新增持续改进循环：`docs/improvement-loop.md` 定义夜间自动改进的提案（双 lane 各 5 方向）、决策框架、设计冻结清单与候选池，运行记录追加在 `docs/research/loop-<月份>.md`；首轮双 lane 提案收敛出采集去重、全局快捷键、内容搜索、撤销回收站、缩略图性能五个共识方向进入候选池。

## 1.8.2

- 修复在复盘页签打字时面板意外收起：中文输入法组合窗/候选窗是独立窗口，出现在指针下方时系统会补发一次假的鼠标离开；此前收起判定只认指针位置和分类改名状态，复盘编辑器没有等价保护。现在“面板内文本编辑控件持有键盘焦点”成为面板状态机的显式保持原因（与拖拽保持同构），编辑器聚焦期间任何指针离开（含候选窗引发的假离开）都不启动或提交自动收起；焦点离开或窗口失活后按既有 250ms 节奏恢复收起，分类改名保护、剪贴板分类收起与拖拽行为不变。

## 1.8.1

- 修复展开时的偶发单帧闪现：窗口几何迁移不再拆成多次系统矩形更新。1.8.0 及之前把一次展开/收起拆成 4 次独立属性赋值，各自触发一次 `SetWindowPos`，当 UI 线程在语句间被打断时，「新尺寸 + 旧位置」的中间矩形会被合成器采样一帧（用户录屏中表现为面板以完整尺寸闪现在收起行位置、底边伸出屏幕）。现在对已创建的窗口用一次原子 `SetWindowPos` 直接应用终态矩形，再对齐 WPF 尺寸/位置账本；展开、收起、拖放轨道揭示三类迁移均生效，贴边几何、边缘裁切、收起防闪烁交接与指针包含行为不变。

## 1.8.0

- 新增设置首版：头部操作区齿轮按钮打开独立设置窗口，包含主题（跟随系统/浅色/深色，立即生效）、动效开关（复用减弱动效降级路径）、开机自启开关、数据目录只读展示与打开、关于（版本与仓库链接）和退出入口（走既有保存退出路径）；偏好以 `preferences.json` 原子持久化，旧安装无此文件时全部取默认值。强制主题后不再跟随系统亮暗变化。
- 贴屏幕右缘的窗口改为边缘裁切：窗口右缘越出屏幕 8px（`WindowSettings.EdgeBleed`），DWM 圆角落在屏外，可见右缘平直、左缘保留圆角；右贴任务栏或右邻显示器时自动回退贴齐，显示器配置变化时自动重估。Mica、两主题、拖动缩放与收起防闪烁行为不变。
- 开机自启开关仅在用户显式切换时写入 HKCU Run 值（与安装器完全相同的值名与带引号路径格式），显示状态始终只读登记；运行安装程序原地更新后自启会重新开启，可在设置中再次关闭。

## 1.7.0

- 平台范围收敛：Windows 以 Windows 11 为开发与验证目标（不再单独适配 Windows 10），Mac 仅维护 Apple Silicon；CI 与默认打包目标同步收敛为 Windows + `osx-arm64`，下载说明同步更新。Intel Mac 的最后可用版本为 1.6.0，历史包与校验文件保留不撤回。
- 界面与动效全面升级：悬停配色、状态浮层、选择框、图钉、头部操作区、已选徽标与卡片显隐全部改为 83–250ms 可中断过渡并跟随系统「减弱动效」降级；新条目 40ms 交错入场、删除并行淡出与失败回滚恢复、拖放指示器淡入；面板收起由硬切换改为 200ms 加速出场（出场中重入即取消，收起防闪烁交接与状态机语义不变）。
- 新增深色主题并跟随系统亮暗自动切换；颜色、圆角、动效时长与缓动收敛为共享核心 `DesignTokens` 唯一数值源（Windows/Mac 两端镜像并由测试锁定），新增设计规范 `docs/design.md`；Mac 端配色统一为与 Windows 一致的紫色系，并同步深色主题与面板进/出场动效（无 Mac 实机人工验收，以 Windows 预览与 CI Apple Silicon 验证代替；Mac 动效暂无「减弱动效」降级）。
- 窗口壳升级为 Windows 11 Mica 材质：弃用分层窗口透明，改用 WindowChrome 玻璃帧 + DWM 材质/系统圆角/沉浸式深色联动；圆角语言改为四角统一 8px，壳表面为半透明 Mica 色调并随深浅主题切换，材质不可用环境自动回退不透明壳。图钉与清空图标换用 Fluent System Icons（MIT）官方几何，卡片获得统一层级阴影与悬停 1px 上浮。新增本机预览取证入口 `FTS_PREVIEW_DATA_DIR` / `FTS_PREVIEW_THEME`（隔离数据目录与独立单实例锁）。
- 将“文本1”改为按天 Markdown 复盘标签：支持今天优先、历史日期列表、700ms 自动保存、空白文件删除，以及来自 Obsidian 的外部修改刷新和三方冲突合并。首次启用会清空该标签已有卡片，其他分类保持不变；Windows 与 Mac 同步提供原生多行编辑器。
- 修复复盘未保存内容在切换分类后丢失的问题：Mac 端切走复盘标签前先冲刷保存，两端待保存内容不再因切换或保存排队被跳过；Mac 冲突对话框被系统关闭按钮关闭时按保留本地处理，不再挂起。
- 复盘文件被占用或无权限时改为提示读取/保存/刷新失败，不再因未处理的异常退出；两端补充 UI 线程兜底异常处理，降低后台刷新链路崩溃风险。
- 修复剪贴板序号获取持续失败（返回 0）的会话中，首次复制之后所有内容被当作重复静默丢弃的问题。
- Windows 拖拽启动时内容已被删除或面板失效会提示后重试，不再崩溃；从外部拖入或复制图片文件时归一化和 PNG 编码移到后台线程，大图不再卡住界面。
- 读取到未知 `board.json` 架构版本（如回装旧版应用）时先保留原文件副本再从空白开始，避免下一次保存覆盖旧数据。
- Mac 端自动轮询撞上剪贴板瞬间变化不再误报“读取失败”；数据目录无权限时单实例锁失败改为安静退出。
- 移除生产不可达的单条移动/恢复 API（`Move`、`MoveAsync`、`Undo(BoardMove)`、`Restore(RemovedBoardItem)`）、仅测试使用的位图归一化入口与外部拖入预检 `CanRead`，以及若干只写不读的存储和恒为默认值的参数；对应测试改写为批量移动与 `Read` 判空的等价形式。
- 复盘编辑期间保存完成不再停留“正在保存…”，保存中继续输入会显示“未保存”并自动续存；切回复盘时若仍有未保存输入，加载不再用磁盘内容覆盖；Mac 端复盘视图下的条目计数不再被分类计数覆盖。
- 工程化：Windows CI 作业增加 30 分钟超时上限；本地 .NET 引导脚本兼容禁用 IE 引擎的 Windows PowerShell；macOS 打包契约测试失败时保留现场目录便于诊断；修正 Unix 路径测试的跳过提示与实际 CI 阵容不一致的问题。

## 1.6.0

- Windows 与 Mac 在同一 main 分支维护，并在同一个 1.6.0 Release 提供 Windows x64、Apple Silicon 和 Intel Mac 下载，首页按平台并列展示入口。
- 新增 macOS 14+ 候选应用，支持 Apple Silicon 和 Intel，提供四分类、改名、选择、批量整理、文字/图片剪贴板采集及原生拖入/拖出。
- Windows 和 Mac 共用分类/排序/置顶、原子存储、图片限制与业务回归；保持 Windows 安装数据目录和原有交互。
- 发布构建入口默认同时生成 Windows 安装包与两种 Mac ZIP，版本统一读取 `version.txt`；CI 汇总 Windows 和两种 Mac 架构的质量门，Mac 启动验证生成真实窗口截图。
- Mac 候选包尚未经过 Apple 公证；原生系统集成验证状态单独记录，不能以 Windows 预览代替 Mac 实机验证。Mac 删除应用默认保留数据，不自动登记登录启动。
- Mac 打包采用内嵌托管程序集、旁置原生库的布局，并按依赖顺序签名；原生 smoke 检查文字、隐私标记、编码图片与文件剪贴板传输，缺少任一证据则质量门失败。

## 1.5.1

- 修复收起交接期间快速重新展开或进入外部拖入状态时，旧回调覆盖新窗口布局的问题；保持面板可见、拖入栏完整和命中区域一致。
- 面板与设置保存的 JSON 序列化和磁盘写入在后台执行，减轻保存期间的界面和动画卡顿；保留保存顺序、失败回滚及原子写入。
- 图片预览同时约束宽高，修复长竖图按宽度放大后占用大量内存的问题；预览保持比例，原图和拖出内容不变。
- 复用最近访问的图片预览，减少滚动和切换分类时重复解码；缓存最多 64 项、估算像素内存 32 MiB，源文件长度或修改时间变化后刷新，删除后失效。

## 1.5.0

- 支持 `Shift + 单击` 按当前显示顺序连续选择卡片，`Ctrl + Shift + 单击` 可追加区间；重复调整终点保持起点，失效起点从当前卡片重新开始。
- 置顶或删除保存期间作出的新选择不会被保存完成后的旧快照覆盖，保存失败仍回滚内容并保留最新选择。

## 1.4.5

- 四个分类的默认名称从上到下调整为“图片、文本1、文本2、待分类”，继续支持双击或 `F2` 改名。
- 升级保留所有已保存的分类名称（包括旧默认名称和空名称）；没有已保存名称时才使用新默认值。

## 1.4.4

### 可靠性与隐私

- 删除正在保存时阻止重复操作，避免第二次点击把删除所选项扩大成清空分类；已排队删除的卡片再被拖动时安全结束操作。
- 自动采集尊重 Windows 剪贴板的禁止历史记录标记，在读取文字、文件和图片前跳过受保护内容。
- 图片表示分别读取并隔离失败，其他格式抛错时仍保留有效图片；照片导入会应用 EXIF 旋转和镜像方向。
- 一次复制多张图片保留来源顺序，只提交一次；损坏文件不影响同批其他可用图片，提交失败则回滚并清理本批副本。
- 图片输入限制为每个编码表示或源文件 64 MiB、6,400 万像素，保留原始分辨率并拒绝超限输入；仅需首帧时不再解码整个动画。
- 合并同序号剪贴板通知；待处理队列最多保留 16 次采集、约 256 MiB 载荷，超限提示稍后重新复制。
- 启动时过滤非法图片路径；图片访问和清理拒绝管理图片目录及其内部的目录联接，避免操作边界外文件。
- 外部拖入结束后同步收起状态，重新悬停当前分类可正常展开。
- 卸载清理失败时保留数据位置登记并提示残留路径；新卸载器核对安装目录归属，防止旧实例清理当前安装的数据。

### 开发维护

- 单项置顶跳过已在正确位置的条目，减少大型分类中的重复查找。
- 集中生产版本来源，发布指南复用打包入口的全量测试结果，保留独立的格式验证和严格构建。
- 增补保存期间重复操作、异常图片表示、隐私标记、资源限额、路径联接和清理失败的回归证据。

## 1.4.3

### 可靠性

- 剪贴板较大的图片表示只有头信息可读、像素解码失败时，继续尝试同次复制的其他有效表示；保持像素面积优先、同面积位图优先，只保存一张 PNG。
- 图片表示回退不吞掉取消或写入错误；全部不可用时保留原失败提示和文件清理语义。

## 1.4.2

### 可靠性

- 安装、数据迁移与卸载只清理已登记的 `Data`，保留同级用户文件和程序文件。
- 单项置顶或清空保存期间切换分类后，保留新分类的选择与删除操作提示。
- 忽略重复、不可读或已取消的剪贴板通知时，仍保持内容处理顺序。
- 开机自启统一由安装器登记，开发启动不再覆盖正式安装的自启路径。

### 开发维护

- 补充隔离的安装清理行为验证与 CI 安装包编译，区分日常构建和正式发布检查。
- 在一次性 Windows CI 虚机验证真实安装、原地重装和卸载，检查数据、自启登记及同级无关文件保留。
- 保持 CI 质量门名称与分支保护一致，避免安装包检查扩充后阻塞正常合并。
- 按职责整理分类编辑、窗口交互测试和生命周期测试，补充历史设计与计划索引。

## 1.4.1

### 可靠性

- 面板收起或编辑分类名称时，`Ctrl + P` 不再修改保留选择。

## 1.4.0

### 新增

- 支持用 `F2` 改名当前展开分类；双击改名、中文输入法、提交与取消语义保持不变。

## 1.3.0

### 新增

- 支持用 `Delete` 键删除当前选择；`Backspace` 保持兼容，分类名称编辑框仍保留原生文字删除语义。

### 可靠性

- 面板收起后，`Delete` 与 `Backspace` 不会删除不可见的保留选择。

## 1.2.0

### 新增

- 支持用 `Esc` 取消当前分类全部选择，分类名称编辑框仍保留取消改名语义。

### 可靠性

- 单卡或批量置顶正在保存时，较晚发生的 `Esc` 取消选择不会再被旧选择快照覆盖。

## 1.1.0

### 新增

- 支持通过顶部图钉或 `Ctrl + P` 对当前选择执行批量置顶与批量取消置顶；混合选择会统一置顶，全已置顶选择会统一取消置顶。
- 支持用 `Ctrl + A` 选择当前分类全部内容，分类名称编辑框仍保留文字全选语义。

### 可靠性

- 批量改变置顶状态只保存一次，并在保存失败时原子恢复每项状态、精确顺序、选择集合和滚动位置。

## 1.0.0

首次 GitHub 公开版本。

### 已有能力

- 自动收集剪贴板中的常见图片和非空文字。
- 四分类整理、分类改名、置顶、多选、批量移动、排序和删除。
- 从外部软件把通用图片或文字拖入指定分类。
- 把单项文字/图片或纯图片批次拖到其他软件。
- 贴边置顶、自动展开收起、滚动位置记忆和本地持久化。
- 可选择程序与数据目录，支持更新迁移、开机自启和完整卸载。

### 1.0 质量整理

- 按职责拆分主窗口实现，减少事件、拖放、删除和动画逻辑的耦合。
- 集中界面颜色、间距、圆角和操作列宽度，减少不同区域的视觉割裂。
- 补强动画中断、系统关闭动画、滚动恢复、删除回滚和拖放失败路径。
- 自动质量门达到 350/350，格式验证和严格 Release 构建通过。

### 已知限制

- 批量置顶与批量取消置顶计划在后续版本设计。
- B-005 与 B-006 仍是自动环境未稳定复现的低优先级现场观察。
