# 更新记录

本项目从 1.0.0 开始公开维护。版本号遵循 `主版本.次版本.修订号`。

## 未发布

- 界面与动效升级：新增深色主题并跟随系统亮暗自动切换（`ImmersiveColorSet` 通知 + `AppsUseLightTheme` 注册表），颜色与卡片阴影拆分为浅/深两套主题字典在运行时整本替换，所有画刷改为动态引用即时生效；深色强调色调整为高对比紫 `#9C8CFF`。
- 界面与动效升级：图钉与清空图标改为微软 Fluent System Icons（MIT）官方几何（pin/delete 20px 线性轮廓），恢复尺寸四角括号、选择对勾与选择框线宽统一为 1.5 圆角端点；图标几何来源由测试锁定。
- 界面与动效升级：卡片获得统一层级阴影（共享冻结投影，虚拟化列表零额外实例化），悬停时 1px 渲染上浮（167ms，不影响布局与命中测试）；滚动条滑块悬停经 6px 悬停层交叉淡入表现展宽。
- 界面与动效升级：新条目与置顶/移动重排的卡片以 40ms 交错步长淡入上浮入场（封顶 8 步）；删除与清空在数据管线运行的同时播放 120ms 卡片淡出，保存失败回滚后卡片恢复并以入场动画重现，不延迟任何数据操作；拖放插入指示器与分类拖放目标层改为淡入淡出。
- 界面与动效升级：悬停配色（头部按钮、分类标签、卡片、置顶/选择按钮、滚动条滑块）与状态浮层、选择框、图钉按钮、头部操作区、已选徽标的显隐全部改为 83–250ms 可中断过渡，跟随系统「减弱动效」立即定格；面板收起由硬切换改为 200ms 加速出场（内容向贴边轨道淡出位移），收起视觉交接与面板状态机语义保持不变，出场期间重入/拖拽/展开会取消本次收起。
- 界面与动效升级（第一阶段）：颜色、圆角、动效时长与缓动收敛为共享核心 `DesignTokens` 唯一数值源，Windows 资源字典与 Mac 常量两端镜像并由测试锁定；新增设计规范 `docs/design.md`。Mac 端配色统一为与 Windows 一致的紫色系（原青绿强调色停用）；清理复盘编辑器与状态浮层的硬编码颜色。浅色主题视觉除 Mac 配色外无变化。
- 将“文本1”改为按天 Markdown 复盘标签：支持今天优先、历史日期列表、700ms 自动保存、空白文件删除，以及来自 Obsidian 的外部修改刷新和三方冲突合并。首次启用会清空该标签已有卡片，其他分类保持不变；Windows 与 Mac 同步提供原生多行编辑器。
- 修复复盘未保存内容在切换分类后丢失的问题：Mac 端切走复盘标签前先冲刷保存，两端待保存内容不再因切换或保存排队被跳过；Mac 冲突对话框被系统关闭按钮关闭时按保留本地处理，不再挂起。
- 复盘文件被占用或无权限时改为提示读取/保存/刷新失败，不再因未处理的异常退出；两端补充 UI 线程兜底异常处理，降低后台刷新链路崩溃风险。
- 修复剪贴板序号获取持续失败（返回 0）的会话中，首次复制之后所有内容被当作重复静默丢弃的问题。
- Windows 拖拽启动时内容已被删除或面板失效会提示后重试，不再崩溃；从外部拖入或复制图片文件时归一化和 PNG 编码移到后台线程，大图不再卡住界面。
- 读取到未知 `board.json` 架构版本（如回装旧版应用）时先保留原文件副本再从空白开始，避免下一次保存覆盖旧数据。
- Mac 端自动轮询撞上剪贴板瞬间变化不再误报“读取失败”；数据目录无权限时单实例锁失败改为安静退出。
- 移除生产不可达的单条移动/恢复 API（`Move`、`MoveAsync`、`Undo(BoardMove)`、`Restore(RemovedBoardItem)`）、仅测试使用的位图归一化入口与外部拖入预检 `CanRead`，以及若干只写不读的存储和恒为默认值的参数；对应测试改写为批量移动与 `Read` 判空的等价形式。
- 复盘编辑期间保存完成不再停留“正在保存…”，保存中继续输入会显示“未保存”并自动续存；切回复盘时若仍有未保存输入，加载不再用磁盘内容覆盖；Mac 端复盘视图下的条目计数不再被分类计数覆盖。
- 工程化：Windows CI 作业增加 30 分钟超时上限；本地 .NET 引导脚本兼容禁用 IE 引擎的 Windows PowerShell；macOS 打包契约测试失败时保留现场目录便于诊断；修正 Unix 路径测试的跳过提示与实际 CI 阵容不一致的问题。

## 1.6.0

- Windows 与 Mac 在同一 main 分支维护，并在同一个 1.6.0 Release 提供 Windows x64、Apple Silicon 和 Intel Mac 下载，首页按平台并列展示入口。
- 新增 macOS 14+ 候选应用，支持 Apple Silicon 和 Intel，提供四分类、改名、选择、批量整理、文字/图片剪贴板采集及原生拖入/拖出。
- Windows 和 Mac 共用分类/排序/置顶、原子存储、图片限制与业务回归；保持 Windows 安装数据目录和原有交互。
- 发布构建入口默认同时生成 Windows 安装包与两种 Mac ZIP，版本统一读取 `version.txt`；CI 汇总 Windows 和两种 Mac 架构的质量门，Mac 启动验证生成真实窗口截图。
- Mac 候选包尚未经过 Apple 公证；原生系统集成验证状态单独记录，不能以 Windows 预览代替 Mac 实机验证。Mac 删除应用默认保留数据，不自动登记登录启动。
- Mac 打包采用内嵌托管程序集、旁置原生库的布局，并按依赖顺序签名；原生 smoke 检查文字、隐私标记、编码图片与文件剪贴板传输，缺少任一证据则质量门失败。

## 1.5.1

- 修复收起交接期间快速重新展开或进入外部拖入状态时，旧回调覆盖新窗口布局的问题；保持面板可见、拖入栏完整和命中区域一致。
- 面板与设置保存的 JSON 序列化和磁盘写入在后台执行，减轻保存期间的界面和动画卡顿；保留保存顺序、失败回滚及原子写入。
- 图片预览同时约束宽高，修复长竖图按宽度放大后占用大量内存的问题；预览保持比例，原图和拖出内容不变。
- 复用最近访问的图片预览，减少滚动和切换分类时重复解码；缓存最多 64 项、估算像素内存 32 MiB，源文件长度或修改时间变化后刷新，删除后失效。

## 1.5.0

- 支持 `Shift + 单击` 按当前显示顺序连续选择卡片，`Ctrl + Shift + 单击` 可追加区间；重复调整终点保持起点，失效起点从当前卡片重新开始。
- 置顶或删除保存期间作出的新选择不会被保存完成后的旧快照覆盖，保存失败仍回滚内容并保留最新选择。

## 1.4.5

- 四个分类的默认名称从上到下调整为“图片、文本1、文本2、待分类”，继续支持双击或 `F2` 改名。
- 升级保留所有已保存的分类名称（包括旧默认名称和空名称）；没有已保存名称时才使用新默认值。

## 1.4.4

### 可靠性与隐私

- 删除正在保存时阻止重复操作，避免第二次点击把删除所选项扩大成清空分类；已排队删除的卡片再被拖动时安全结束操作。
- 自动采集尊重 Windows 剪贴板的禁止历史记录标记，在读取文字、文件和图片前跳过受保护内容。
- 图片表示分别读取并隔离失败，其他格式抛错时仍保留有效图片；照片导入会应用 EXIF 旋转和镜像方向。
- 一次复制多张图片保留来源顺序，只提交一次；损坏文件不影响同批其他可用图片，提交失败则回滚并清理本批副本。
- 图片输入限制为每个编码表示或源文件 64 MiB、6,400 万像素，保留原始分辨率并拒绝超限输入；仅需首帧时不再解码整个动画。
- 合并同序号剪贴板通知；待处理队列最多保留 16 次采集、约 256 MiB 载荷，超限提示稍后重新复制。
- 启动时过滤非法图片路径；图片访问和清理拒绝管理图片目录及其内部的目录联接，避免操作边界外文件。
- 外部拖入结束后同步收起状态，重新悬停当前分类可正常展开。
- 卸载清理失败时保留数据位置登记并提示残留路径；新卸载器核对安装目录归属，防止旧实例清理当前安装的数据。

### 开发维护

- 单项置顶跳过已在正确位置的条目，减少大型分类中的重复查找。
- 集中生产版本来源，发布指南复用打包入口的全量测试结果，保留独立的格式验证和严格构建。
- 增补保存期间重复操作、异常图片表示、隐私标记、资源限额、路径联接和清理失败的回归证据。

## 1.4.3

### 可靠性

- 剪贴板较大的图片表示只有头信息可读、像素解码失败时，继续尝试同次复制的其他有效表示；保持像素面积优先、同面积位图优先，只保存一张 PNG。
- 图片表示回退不吞掉取消或写入错误；全部不可用时保留原失败提示和文件清理语义。

## 1.4.2

### 可靠性

- 安装、数据迁移与卸载只清理已登记的 `Data`，保留同级用户文件和程序文件。
- 单项置顶或清空保存期间切换分类后，保留新分类的选择与删除操作提示。
- 忽略重复、不可读或已取消的剪贴板通知时，仍保持内容处理顺序。
- 开机自启统一由安装器登记，开发启动不再覆盖正式安装的自启路径。

### 开发维护

- 补充隔离的安装清理行为验证与 CI 安装包编译，区分日常构建和正式发布检查。
- 在一次性 Windows CI 虚机验证真实安装、原地重装和卸载，检查数据、自启登记及同级无关文件保留。
- 保持 CI 质量门名称与分支保护一致，避免安装包检查扩充后阻塞正常合并。
- 按职责整理分类编辑、窗口交互测试和生命周期测试，补充历史设计与计划索引。

## 1.4.1

### 可靠性

- 面板收起或编辑分类名称时，`Ctrl + P` 不再修改保留选择。

## 1.4.0

### 新增

- 支持用 `F2` 改名当前展开分类；双击改名、中文输入法、提交与取消语义保持不变。

## 1.3.0

### 新增

- 支持用 `Delete` 键删除当前选择；`Backspace` 保持兼容，分类名称编辑框仍保留原生文字删除语义。

### 可靠性

- 面板收起后，`Delete` 与 `Backspace` 不会删除不可见的保留选择。

## 1.2.0

### 新增

- 支持用 `Esc` 取消当前分类全部选择，分类名称编辑框仍保留取消改名语义。

### 可靠性

- 单卡或批量置顶正在保存时，较晚发生的 `Esc` 取消选择不会再被旧选择快照覆盖。

## 1.1.0

### 新增

- 支持通过顶部图钉或 `Ctrl + P` 对当前选择执行批量置顶与批量取消置顶；混合选择会统一置顶，全已置顶选择会统一取消置顶。
- 支持用 `Ctrl + A` 选择当前分类全部内容，分类名称编辑框仍保留文字全选语义。

### 可靠性

- 批量改变置顶状态只保存一次，并在保存失败时原子恢复每项状态、精确顺序、选择集合和滚动位置。

## 1.0.0

首次 GitHub 公开版本。

### 已有能力

- 自动收集剪贴板中的常见图片和非空文字。
- 四分类整理、分类改名、置顶、多选、批量移动、排序和删除。
- 从外部软件把通用图片或文字拖入指定分类。
- 把单项文字/图片或纯图片批次拖到其他软件。
- 贴边置顶、自动展开收起、滚动位置记忆和本地持久化。
- 可选择程序与数据目录，支持更新迁移、开机自启和完整卸载。

### 1.0 质量整理

- 按职责拆分主窗口实现，减少事件、拖放、删除和动画逻辑的耦合。
- 集中界面颜色、间距、圆角和操作列宽度，减少不同区域的视觉割裂。
- 补强动画中断、系统关闭动画、滚动恢复、删除回滚和拖放失败路径。
- 自动质量门达到 350/350，格式验证和严格 Release 构建通过。

### 已知限制

- 批量置顶与批量取消置顶计划在后续版本设计。
- B-005 与 B-006 仍是自动环境未稳定复现的低优先级现场观察。
