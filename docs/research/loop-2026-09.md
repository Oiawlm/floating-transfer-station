# 持续改进循环记录 · 2026-09

每晚循环的提案、决策与结果按日期追加。规则见 [持续改进循环](../improvement-loop.md)。

## 2026-09-24(首轮,主会话执行)

**提案(wbx 双 lane fanout,job `20260924-221324-vae`,两 lane 均一次成功)**

两 lane 各 5 个方向,收敛度极高,合并后 5 个共识方向:

1. 采集去重 + 微信复制图片偶发双份修复(两 lane 均列首位)
2. 全局快捷键唤起 + 键盘直达(唤出/贴出最近/数字键取用)
3. 内容搜索与筛选(只读视图,保持置顶分区)
4. 撤销与回收站(Ctrl+Z、批量恢复、容量上限)
5. 缩略图异步解码/缓存 + 分类切换即时反馈

排序分歧:ai 建议 先去重→性能→快捷键→搜索→撤销;cn 建议 先去重→快捷键→性能→撤销→搜索。备选:上手引导、自动更新、整站导出备份、保留策略。

**决策**

首轮执行用户指定方向:插件系统首版(垂直切片)。理由:用户明确要求"现在就可以马上动手做";双 lane 共识方向全部进入候选池,由后续夜间循环按决策框架逐项消化(去重与快捷键为下轮优先)。

**实现**

- Core:`PluginManifest`(声明式清单 + 严格校验)、`PluginCatalog`(双目录发现、覆盖语义、原子状态持久化、管线快照)、`TextCleanerPipeline`(按序应用、失败/清空回退原文)。
- WPF:两个采集入口共用同一清理管线(每次采集取最新快照,开关即时生效);设置窗口新增插件区块(名称/版本/描述/错误原因/开关/插件目录入口);内建示例插件 `whitespace-tidy`。
- 测试:Core 14 项(解析、发现、覆盖、状态、回退、管线顺序)+ 设置窗口 STA 4 项(渲染、开关生效持久化、坏清单、无目录降级)。
- 文档:README、ROADMAP、CHANGELOG、docs/README.md、improvement-loop.md(循环规则与冻结清单)。

**结果**:见 CHANGELOG 对应版本记录。夜间定时任务(每晚 23:15)已建立,自次日起自动执行。

## 2026-09-25(整点接力首夜,切片 1:采集去重 + B-006,1.9.1)

**方向与决策**

整点接力机制首夜(23:00–09:00,锁 + checkpoint + 整点兜底触发)。直接选取候选池双 lane 均列第 1 的"采集去重 + 微信复制图片偶发双份修复"(B-006),未再重复发散提案——候选池高优先项可直接选用。根因定位:微信等来源一次复制分多步写剪贴板,产生多个**不同序号**的通知,原有按 `GetClipboardSequenceNumber` 的去重无法合并;修复取内容级指纹而非继续纠缠序号。

**实现**

- Core 新增 `CaptureDeduplicationGate`(只记最近一条成功采集,默认 5 秒窗口,时钟可注入)与 `CapturedContentFingerprint`(文本 = 整理插件处理后全文 SHA-256;图片 = 规范化后 PNG 解码像素 + 尺寸的 SHA-256——按像素而非编码字节,位图与文件路径等不同表示之间可互认;解码失败返回 null 即放弃去重,绝不影响采集)。
- `ClipboardCaptureService` 注入可选去重门(默认 null 保持旧行为,既有对抗测试锁定"无门不抑制"契约):检查与记录都在 `BoardOperationGate` 内串行,消除双通知竞态;抑制时不入库、删除已规范化文件并提示"与最近收集的内容相同,未重复收集。";批量多条导入与外部拖入永不因重复被抑制;保存失败不记指纹。
- Mac 端遵守暂停边界:共享 Core 已具备能力,采集接线待恢复;Mac 测试(36 项)与 Apple Silicon CI 全绿。

**委派(wbx fanout,job `20260925-001458-1um`,双 lane 均 1 次成功)**

- ai lane:Core 去重门 MSTest 单元测试草稿 → 审查后采纳 15 项(删 1 处 TODO 注释,改 1 处写法),即 `CaptureDeduplicationGateTests`。
- cn lane:CHANGELOG/README/B-006 状态三段文案草稿 → 采纳并补充 Mac 状态句。

**测试与质量门**

- 新增 21 项(Core 14 + 服务级 7,含真实 `ImageNormalizer` 的跨表示互认、抑制后文件清理、失败不记指纹、批量不去重)。
- 质量门四连通过;全量 Release 测试 784 项通过(0 失败)。
- 隔离实例(`FTS_PREVIEW_DATA_DIR`)真实剪贴板三轮验证:每对 1.2–1.3 秒内两次相同复制恰好只收集一条(板计数 2→3→4→5,共 8 次复制收 5 条)。
- **取证教训(如实记录)**:状态浮层文本未能在截图中定位——两个实例窗口(用户 1.9.0 + 预览)边缘贴条完全重叠且被终端内容遮挡,Windows OCR 只读到终端文字;截图已存 `artifacts/nightly-20260925-dedup-suppressed-status.png`,浮层本身为既有 UI,本次仅新增字符串。后续单实例环境可补拍。

**CI 与发行**

- 首次 CI 失败:README 安装包名仍指 1.9.0(version.txt 已升 1.9.1)——本地全量测试先于版本提升运行,顺序失误;补 README/PROJECT_GUIDE 同步后全绿。**教训:版本提升与 README 下载表必须同一次提交。**
- PR #46 合并 main(848141e),main 质量门运行 36029942114 全绿,产物直接用于发行。
- 统一发行 [v1.9.1](https://github.com/Oiawlm/floating-transfer-station/releases/tag/v1.9.1):Windows 安装包 + Apple Silicon ZIP + SHA256SUMS.txt + Mac 验证元数据,Mac 如实标注未公证测试状态。

**装机结果**

- 优雅关闭(WM_CLOSE)路径完成保存,但进程在窗口销毁后又滞留约 30 秒才消失,最终强制结束——见待裁决。
- 本机静默安装:第一次运行卡在安装前阶段(CloseApplications/Restart Manager 疑似挂起,10 分钟无进展,无人占用安装目录模块),终止后重跑一次即成功(90 秒内退出码 0);安装目录、数据目录登记与自启注册表全部完好,重启后进程 1.9.1.0 验证通过。
- **用户板污染与清理(如实记录)**:取证期间 8 次 `Set-Clipboard` 被运行中的 1.9.0 实例全部收集(其中单次复制多次序号变化即产生多条——恰为 B-006 问题的现场实证),共 12 条标记测试卡;优雅关闭后已从 `board.json` 精确移除(29→17 条,逐条按标记文本匹配,备份存本机 artifacts/ 未进仓库)。1.9.1 上线后此类取证副本将自然去重为单条。

**待裁决(明早请用户过目)**

1. 优雅关闭后进程滞留约 30 秒(窗口已销毁、保存已完成)——是否立案调查关闭路径的最终退出阻塞。
2. 安装器首次静默运行疑似卡在 Restart Manager(重试即成功)——是否在脚本层增加对策或仅在复发时处理。
3. 状态浮层截图取证在多实例重叠环境下不可行——是否接受"数据面证据 + 既有浮层"的取证口径。
