# 持续改进循环记录 · 2026-09

每晚循环的提案、决策与结果按日期追加。规则见 [持续改进循环](../improvement-loop.md)。

## 2026-09-24(首轮,主会话执行)

**提案(wbx 双 lane fanout,job `20260924-221324-vae`,两 lane 均一次成功)**

两 lane 各 5 个方向,收敛度极高,合并后 5 个共识方向:

1. 采集去重 + 微信复制图片偶发双份修复(两 lane 均列首位)
2. 全局快捷键唤起 + 键盘直达(唤出/贴出最近/数字键取用)
3. 内容搜索与筛选(只读视图,保持置顶分区)
4. 撤销与回收站(Ctrl+Z、批量恢复、容量上限)
5. 缩略图异步解码/缓存 + 分类切换即时反馈

排序分歧:ai 建议 先去重→性能→快捷键→搜索→撤销;cn 建议 先去重→快捷键→性能→撤销→搜索。备选:上手引导、自动更新、整站导出备份、保留策略。

**决策**

首轮执行用户指定方向:插件系统首版(垂直切片)。理由:用户明确要求"现在就可以马上动手做";双 lane 共识方向全部进入候选池,由后续夜间循环按决策框架逐项消化(去重与快捷键为下轮优先)。

**实现**

- Core:`PluginManifest`(声明式清单 + 严格校验)、`PluginCatalog`(双目录发现、覆盖语义、原子状态持久化、管线快照)、`TextCleanerPipeline`(按序应用、失败/清空回退原文)。
- WPF:两个采集入口共用同一清理管线(每次采集取最新快照,开关即时生效);设置窗口新增插件区块(名称/版本/描述/错误原因/开关/插件目录入口);内建示例插件 `whitespace-tidy`。
- 测试:Core 14 项(解析、发现、覆盖、状态、回退、管线顺序)+ 设置窗口 STA 4 项(渲染、开关生效持久化、坏清单、无目录降级)。
- 文档:README、ROADMAP、CHANGELOG、docs/README.md、improvement-loop.md(循环规则与冻结清单)。

**结果**:见 CHANGELOG 对应版本记录。夜间定时任务(每晚 23:15)已建立,自次日起自动执行。

## 2026-09-25(整点接力首夜,切片 1:采集去重 + B-006,1.9.1)

**方向与决策**

整点接力机制首夜(23:00–09:00,锁 + checkpoint + 整点兜底触发)。直接选取候选池双 lane 均列第 1 的"采集去重 + 微信复制图片偶发双份修复"(B-006),未再重复发散提案——候选池高优先项可直接选用。根因定位:微信等来源一次复制分多步写剪贴板,产生多个**不同序号**的通知,原有按 `GetClipboardSequenceNumber` 的去重无法合并;修复取内容级指纹而非继续纠缠序号。

**实现**

- Core 新增 `CaptureDeduplicationGate`(只记最近一条成功采集,默认 5 秒窗口,时钟可注入)与 `CapturedContentFingerprint`(文本 = 整理插件处理后全文 SHA-256;图片 = 规范化后 PNG 解码像素 + 尺寸的 SHA-256——按像素而非编码字节,位图与文件路径等不同表示之间可互认;解码失败返回 null 即放弃去重,绝不影响采集)。
- `ClipboardCaptureService` 注入可选去重门(默认 null 保持旧行为,既有对抗测试锁定"无门不抑制"契约):检查与记录都在 `BoardOperationGate` 内串行,消除双通知竞态;抑制时不入库、删除已规范化文件并提示"与最近收集的内容相同,未重复收集。";批量多条导入与外部拖入永不因重复被抑制;保存失败不记指纹。
- Mac 端遵守暂停边界:共享 Core 已具备能力,采集接线待恢复;Mac 测试(36 项)与 Apple Silicon CI 全绿。

**委派(wbx fanout,job `20260925-001458-1um`,双 lane 均 1 次成功)**

- ai lane:Core 去重门 MSTest 单元测试草稿 → 审查后采纳 15 项(删 1 处 TODO 注释,改 1 处写法),即 `CaptureDeduplicationGateTests`。
- cn lane:CHANGELOG/README/B-006 状态三段文案草稿 → 采纳并补充 Mac 状态句。

**测试与质量门**

- 新增 21 项(Core 14 + 服务级 7,含真实 `ImageNormalizer` 的跨表示互认、抑制后文件清理、失败不记指纹、批量不去重)。
- 质量门四连通过;全量 Release 测试 784 项通过(0 失败)。
- 隔离实例(`FTS_PREVIEW_DATA_DIR`)真实剪贴板三轮验证:每对 1.2–1.3 秒内两次相同复制恰好只收集一条(板计数 2→3→4→5,共 8 次复制收 5 条)。
- **取证教训(如实记录)**:状态浮层文本未能在截图中定位——两个实例窗口(用户 1.9.0 + 预览)边缘贴条完全重叠且被终端内容遮挡,Windows OCR 只读到终端文字;截图已存 `artifacts/nightly-20260925-dedup-suppressed-status.png`,浮层本身为既有 UI,本次仅新增字符串。后续单实例环境可补拍。

**CI 与发行**

- 首次 CI 失败:README 安装包名仍指 1.9.0(version.txt 已升 1.9.1)——本地全量测试先于版本提升运行,顺序失误;补 README/PROJECT_GUIDE 同步后全绿。**教训:版本提升与 README 下载表必须同一次提交。**
- PR #46 合并 main(848141e),main 质量门运行 36029942114 全绿,产物直接用于发行。
- 统一发行 [v1.9.1](https://github.com/Oiawlm/floating-transfer-station/releases/tag/v1.9.1):Windows 安装包 + Apple Silicon ZIP + SHA256SUMS.txt + Mac 验证元数据,Mac 如实标注未公证测试状态。

**装机结果**

- 优雅关闭(WM_CLOSE)路径完成保存,但进程在窗口销毁后又滞留约 30 秒才消失,最终强制结束——见待裁决。
- 本机静默安装:第一次运行卡在安装前阶段(CloseApplications/Restart Manager 疑似挂起,10 分钟无进展,无人占用安装目录模块),终止后重跑一次即成功(90 秒内退出码 0);安装目录、数据目录登记与自启注册表全部完好,重启后进程 1.9.1.0 验证通过。
- **用户板污染与清理(如实记录)**:取证期间 8 次 `Set-Clipboard` 被运行中的 1.9.0 实例全部收集(其中单次复制多次序号变化即产生多条——恰为 B-006 问题的现场实证),共 12 条标记测试卡;优雅关闭后已从 `board.json` 精确移除(29→17 条,逐条按标记文本匹配,备份存本机 artifacts/ 未进仓库)。1.9.1 上线后此类取证副本将自然去重为单条。

**待裁决(明早请用户过目)**

1. 优雅关闭后进程滞留约 30 秒(窗口已销毁、保存已完成)——是否立案调查关闭路径的最终退出阻塞。1.10.0 装机时同一关闭路径 25 秒内正常退出,倾向一次性现象,暂不立案。
2. 安装器首次静默运行疑似卡在 Restart Manager(重试即成功)——是否在脚本层增加对策或仅在复发时处理。1.10.0 安装一次性成功,倾向观察。
3. 状态浮层截图取证在多实例重叠环境下不可行——是否接受"数据面证据 + 既有浮层"的取证口径。

## 2026-09-25(整点接力首夜,切片 2:全局快捷键唤起,1.10.0)

**方向与决策**

候选池下一个双 lane 共识项"全局快捷键唤起与键盘直达"的最小垂直切片:只做"唤起"(Ctrl + Alt + V 展开面板到默认接收分类),"键盘直达(贴出最近一条/数字键取用)"语义待设计,保留在候选池。决策要点:与开机自启同语义——默认关闭、仅显式开启才注册系统热键(避免与其他软件冲突);组合键固定 Ctrl + Alt + V(系统无既有占用),可配置性留待后续。

**实现**

- `Win32GlobalHotkeySource`(RegisterHotKey/UnregisterHotKey)+ `IGlobalHotkeySource` 抽象(测试替身);`NativeMethods` 增补 WM_HOTKEY 与 P/Invoke。
- MainWindow:注入热键源;SourceInitialized 按偏好注册(失败仅状态条提示、不改写偏好,下次启动重试);WndProc 仅响应本应用热键 id(其他软件热键不误触);`OnGlobalHotkeyPressed` 展开面板到默认接收分类,已展开或拖放中不打断;Closing 注销。
- 设置窗口新增"全局快捷键"区块:开关默认关闭;开启先注册、成功才落偏好;占用冲突提示"全局快捷键注册失败,可能被其他软件占用。"并回退关闭。
- 共享偏好模型 `AppPreferences` 新增 `GlobalHotkeyEnabled`(默认 false,旧 JSON 缺字段回落默认);Mac 端仅共享模型字段,界面接入待恢复。

**委派(wbx fanout,job `20260925-012556-2ae`,ai lane 1 次成功)**

CHANGELOG 与 README 设置条目文案草稿 → 与自写版本交叉核对,事实一致,采用信息更全的自写版本(含 Mac 说明)。

**测试与质量门**

- 交互测试 5 项(默认关闭不注册、开关即注册/注销并持久化、注册失败回退且不写偏好、启动注册+关闭注销、热键消息展开面板+无关热键 id 防误触)+ Core 偏好兼容 3 项;全量 792 项通过,质量门四连通过,Mac 测试与 Apple Silicon CI 无回归。
- 设置窗双主题真实渲染截图(`FTS_SETTINGS_EVIDENCE_DIR` → artifacts/ui-evidence-hotkey,OCR 确认新区块渲染)。
- 真机端到端:隔离实例预置偏好启动,PowerShell 抢注 Ctrl+Alt+V 得 error 1409(已被本应用注册的实证);SendKeys 发送组合键后窗口宽度由 66px 收起条展开至 1280px(40ms 轮询捕捉)。展开面板的截图因预览实例窗口未置顶被终端遮挡未能定格(与切片 1 浮层取证同类限制),行为证据充分。

**CI、发行与装机**

- PR #47 首轮 CI 全绿(吸取切片 1 教训:版本提升与 README 下载表同批提交,无返工)。合并 main(f0413c0),main 质量门 36035131368 全绿。
- 统一发行 [v1.10.0](https://github.com/Oiawlm/floating-transfer-station/releases/tag/v1.10.0)(Windows + Apple Silicon + SHA256SUMS + Mac 元数据)。
- 本机:优雅关闭 25 秒内正常退出(切片 1 的滞留未复现);静默安装一次成功(90 秒内退出码 0);自启与目录登记完好;重启后进程 1.10.0.0 验证通过。
## 2026-09-25(整点接力首夜,切片 3:缩略图异步解码,1.10.1)

**方向与决策**

候选池共识项"缩略图异步解码/缓存 + 分类切换即时反馈"的最小垂直切片(缓存能力此前已有,只做异步解码)。设计约束:转换器已有 20 项锁定行为的测试,采用**零破坏**方案——转换器只新增只读缓存查询接口,不动既有路径;卡片模板仅替换一个元素。

**实现**

- `AsyncThumbnailImage : Image`:缓存命中同步设置已解码缩略图(零 UI 线程开销);未命中清空占位,在与 `ImageThumbnailConverter` 完全共享的缓存上后台解码,完成后回 UI 线程设置冻结位图;快速切换绑定路径只应用最新一代结果;失败/缺失保持占位。
- `ImageThumbnailConverter` 新增 `TryGetCachedThumbnail`(命中且文件未变化才返回;缺失文件清理条目,与 Convert 语义一致)。
- 模板替换 `Image`+转换器绑定为 `AsyncThumbnailImage`,Kind 可见性触发器与布局不变。

**委派**:本切片为单控件收敛实现,核心契约与关键路径自研,未外包(接口小、上下文强耦合,外包审查成本高于收益;连续两次 fanout 已在切片 1/2 完成)。

**测试与质量门**

- 新增 7 项(缓存查询 3 + 控件 4,含同步泵模式替代 async STA 混用——初版 async+PushFrame 死锁,已改纯同步泵并如实记录)。
- 既有 20 项转换器测试与视觉过渡回归原样通过;全量 799 项,质量门四连通过。
- 取证:`FTS_VISUAL_TRANSITIONS_EVIDENCE_DIR` / `FTS_CLIPBOARD_IMAGE_FALLBACK_EVIDENCE_DIR` 真实渲染截图;像素分析确认合成灰图缩略图实际渲染(127 灰以 50% 入场动画混合呈现为 191,首版误判为空白,复核后确认渲染正常)。

**CI、发行与装机**

- PR #48 首轮 CI 全绿,合并 main(7c0145f),main 质量门 36040134053 全绿,统一发行 [v1.10.1](https://github.com/Oiawlm/floating-transfer-station/releases/tag/v1.10.1)。
- 本机:优雅关闭正常退出(连续第二次无滞留),静默安装一次成功,自启与目录登记完好,重启后 1.10.1.0(PID 41432)。
- 关联观察 B-005 已更新为"已上线针对性缓解,待现场确认"。

## 2026-09-25(整点接力首夜,切片 4:分类标签字体清晰度,1.10.2)

**方向与决策**

用户指定方向"分类标签字体清晰度优化(WPF 文本渲染)"。根因:12px 小字号文本按 WPF 默认 Ideal 格式化模式落在亚像素位置,中文笔画发虚。修复:标签与原地改名编辑框样式增加 `TextFormattingMode=Display` + `TextRenderingMode=ClearType`——经典小字号像素对齐方案;仅改渲染选项,字号/字重/配色/命中区域零变化。

**实现与委派**:两个样式各两行 Setter + 一项锁定测试;范围极小,自研未外包。

**测试与质量门**

- 新增回归测试锁定两个样式的渲染设置(防止回退);全量 800 项,质量门四连通过。
- 前后真实渲染取证(`FTS_CATEGORY_NAMES_EVIDENCE_DIR`):修复前后逐像素对比,差异恰好落在分类标签竖条区域(36px 宽,573-584 像素),其余零变化——证明只影响标签渲染。

**CI、发行与装机**

- 首轮 CI 两项失败:① ImageCard 缩略图断言与 1.10.1 异步解码的竞态(本地快机跑赢、CI 慢机暴露)——本分支补泵等待修复;② PluginToggle 临时文件清理 IOException,与本分支无关(仅样式改动),次轮未复现,判定时序抖动,已记录。
- PR #49 合并 main(d877c82),main 质量门 36043246870 全绿,统一发行 [v1.10.2](https://github.com/Oiawlm/floating-transfer-station/releases/tag/v1.10.2);本机静默安装一次成功,自启完好,重启后 1.10.2.0(PID 51684)。

## 2026-09-25(整点接力首夜,切片 5:L 级方向设计文档,纯文档)

**方向与决策**

候选池剩余方向均为 L 级或语义未定,按决策框架"L 级方向先产出设计文档进候选池,不当晚硬做":为「内容搜索与筛选」「撤销与回收站」产出设计草案;「分类标签内容直接编辑」语义不明(现有 F2/双击改名已存在),列入待裁决。

**委派(wbx fanout,job `20260925-025301-yt8`,双 lane 各 1 次成功)**

ai lane 起草搜索设计、cn lane 起草撤销设计;主代理逐行评审。**关键评审修订**:撤销草案原文建议 Ctrl+Z 复用全局热键基建——已纠正为"面板聚焦快捷键,绝不全局注册"(全局注册会抢占所有应用的撤销键);其余 UNKNOWN 标注保留待实施时确定。

**结果**

- 两份草案落盘 `docs/superpowers/specs/2026-09-25-*` 并入候选池与文档索引;PR #50 CI 全绿合并 main(cc21e17)。
- 顺带文档卫生:修正索引表四个已发布功能的"未发布"标注(复盘 1.7.0/动效 1.7.0/边缘裁切 1.8.0/闪现修复 1.8.1)。
- 纯文档切片不发版(无代码与产物变化,版本保持 1.10.2),原因已在此记录。

## 2026-09-25(整点接力首夜,切片 6:Ctrl+Z 撤销最近删除,1.11.0)

**方向与决策**

按切片 5 产出的撤销设计草案实施切片 1(面板聚焦 Ctrl+Z 会话级撤销)。实现中发现设计关键点:既有 `Restore` 是**整类替换**语义,延迟撤销会覆盖删除后的新增/移动/重新置顶——必须新做**锚点插回**。

**实现**

- `BoardService.RestoreInsert`:恢复项优先回到删除前同区(置顶/普通)前驱之后,前驱缺失或已换区回退该区顶部,批内保持删除时显示顺序;链式锚点只在同区内生效,杜绝普通内容混入置顶区。
- `BoardMutationService`:会话级撤销栈(20 批),删除/清空持久化成功后入栈;图片文件保留到批次驱逐、显式丢弃(`DiscardUndoableDeletes`)或正常退出;撤销保存失败经 `RemoveMany` 回到删除状态;空栈提示。
- WPF:Ctrl+Z 键位门与 F2/Delete 同构(面板展开 + 无文本编辑焦点),**绝不注册为全局热键**;关闭序列保存成功后丢弃撤销栈清理文件。Mac:退出路径同步对齐(否则 Mac 删除的图片文件会永久泄漏)。
- 冻结清单新增第 11 项(撤销语义)。

**委派**:核心契约(锚点算法、栈生命周期、回滚路径)自研;本切片无外包。

**测试与质量门**

- 新增服务级 9 项 + 窗口级 1 项;4 个既有断言随"文件清理延迟到撤销栈生命周期"语义更新;全量 810 项,质量门四连通过。初版 4 个测试预期按"追加式新增"推演错误——`AddText` 实为置顶插入,修正预期后全过(产品行为本身正确)。

**CI、发行与装机(两轮修复,如实记录)**

- 首轮 CI:macOS 冒烟失败——原生剪贴板验证的清理段断言"删除后无图片副本残留",与撤销栈文件保留语义冲突;且 Mac 退出路径未丢弃撤销栈(文件泄漏)。最小修复:冒烟清理段按退出语义 Discard;Mac 关闭保存成功后对齐丢弃。次轮全绿。
- main 门再遇插件开关测试失败(第二次,确认为持久化竞态非偶发):测试只等内存生效就重载状态文件,慢盘上原子写未落盘。根治:启用/禁用两侧泵等"重读即所见状态";本地连跑 5 次稳定。PR #52 合并(ffc4ac7)。
- 统一发行 [v1.11.0](https://github.com/Oiawlm/floating-transfer-station/releases/tag/v1.11.0)(产物取 ffc4ac7 的 main 门运行 36050495568);本机静默安装一次成功,自启完好,重启后 1.11.0.0(PID 23448)。

## 2026-09-25(整点接力首夜,切片 7:键盘直达设计文档,纯文档)

**方向与决策**:为候选池最后一个未设计方向产出设计草案,三个未实施方向(内容搜索/回收站/键盘直达)至此全部有设计草案,后续夜间循环可直接按草案取 M 切片实施。

**委派(wbx fanout,job `20260925-035548-uyr`,cn lane 1 次成功)**:评审注记自写剪贴板回捕与 1.9.1 去重窗口的联动风险;"最近一条"语义歧义(显示顺序第一位 vs 最新收集)列为实施前裁决点。PR #53 合并(f74559f),纯文档不发版(版本保持 1.11.0)。

---

# 2026-09-25 整夜汇总

**切片与版本**:7 个切片,4 次统一发行——1.9.1(采集去重+B-006)、1.10.0(全局快捷键唤起)、1.10.1(缩略图异步解码)、1.10.2(标签字体清晰度)、1.11.0(撤销最近删除,含设计草案两份);另有两份纯文档 PR(设计草案,不发版)。全部走完整 PR 质量门合并 main,产物取 main 门运行,Windows+Apple Silicon 全绿后发行;本机每次静默更新并验证版本与自启。

**委派情况**:wbx fanout 6 个 job 全部成功(测试草稿、文案、三份设计草案),无失败无降级;产物均经逐行审查与修订后采用(关键修订:Ctrl+Z 绝不全局注册)。

**失败与修复(如实)**:CI 三次失败——①README 版本同步遗漏(流程失误,已改为版本+README 同批提交);②macOS 冒烟与撤销栈文件保留语义冲突+Mac 退出泄漏(最小修复);③插件开关测试持久化竞态(两次失败后根治)。

**提前收尾原因(04:05)**:剩余候选(内容搜索切片1、回收站切片2+、键盘直达实施)均需在冻结相邻的面板 chrome 上新增 UI 或有实施前裁决点;无人值守深夜时段按"拿不准选风险更小做法"原则不再开新片,候选池已有完整设计草案待后续夜间或用户在场时实施。

**待裁决清单(明早请用户过目)**

1. 「分类标签内容直接编辑」语义不明:现有 F2/双击改名已覆盖改名;该候选指的是什么(收起条上直接编辑?其他)?需要用户说明。
2. 键盘直达「贴出最近一条」的内容来源:当前展开分类 vs 默认接收分类(设计草案 UNKNOWN,实施前裁决)。
3. 键盘直达键位:反引号 vs `Ctrl+Shift+C`(后者与部分软件截图区域冲突),建议实施时实测占用后定。
4. 优雅关闭后进程滞留(仅 1.9.1 装机时出现一次,后续三次未复现)——暂不立案,复发再查。
5. 取证口径:多实例/被遮挡环境下状态浮层与展开态截图不可拍,已以数据面证据+既有渲染通道截图代替(切片 1、2);后续可在单实例环境补拍。
6. 检查 8 个 PR 合并后 main 状态与 4 个 Release 资产完整性(已验证,见各切片条目);如需复核:`git log --oneline 3f3eea3..HEAD` 与 Releases 页。

## 2026-09-25(日间执行窗口,拖拽指示条闪烁 + 卡片文字清晰度,1.11.3)

**方向与决策**:执行 2026-09-25 规划窗口定稿的两个用户报障修复(交接提示词见 `docs/handoff/2026-09-25-indicator-flicker-text-clarity-execution-prompt.md`):Bug A 分类内拖动时插入指示条在置顶边界闪烁/空白区跳变(根因:命中半区+分区校验在边界中线交替隐藏;空白区回退跳末尾),Bug B 卡片正文文字发糊(主因:卡片 DropShadowEffect 把整棵子树渲染进中间位图关闭 ClearType)。修复方案按规划定稿实施,未重新发散。

**实现**

- Bug A(PR #58,`fix/nightly-20260925-insert-indicator-stability`):共享 Core 新增 `InsertionSlotResolver` 纯函数——对已实现条目按竖直中线翻转解析插入槽位(单调连续,中线归下半区与既有 `ForTarget` 语义一致)+ 分区钳制;WPF `GetBoardDropLocation` 重写为几何枚举+钳制,删除回退跳末尾逻辑(`GetVisibleListEndY` 移除);混合置顶批量语义不变。
- Bug B(PR #59,`fix/nightly-20260925-card-text-clarity`):卡片模板三层重构(根容器外距/Lift + 独立阴影层不承载内容不参与命中 + 内容面无 Effect);小字号 Display(正文/复盘编辑器 14、状态文本 12、徽标 9);行高 18→20、MaxHeight 90→100;2d `UseLayoutRounding` 经取证判定无需启用。
- 文档 PR #57 落盘 spec/plan/handoff;`docs/design.md` 层级节补记三层结构与像素对齐契约;CHANGELOG 含指示条契约变更(跨区隐藏+None → 钳制槽+Move)与卡片高度 ±10 DIP 说明。

**测试与质量门**

- 失败先行:Bug A 4 个新 STA 测试(边界 ±1 DIP 抖动持续可见且位置稳定、空白区几何一致、跨区钳制槽、Drop 与指示条一致)+ Core 18 项单测(共享进 Core.Tests 供 Apple Silicon 覆盖);Bug B 3 个新 STA 测试(文字祖先无 Effect、Display 锁定、行高/最大高度)。全部在修复前失败、修复后转绿;3 个既有测试按新结构适配(卡外距断言移到根容器、共享冻结阴影断言移到阴影层、行高契约更新)。
- main 质量门四连+hygiene 全绿(Windows 655、Core 168+5 跳过、Mac 36);PR #59 与 #58 的 CHANGELOG 冲突本地解决;PR #60 首轮 CI 失败——发布材料契约要求 CHANGELOG 永远保留空「未发布」小节,补齐后全绿(如实记录,流程失误)。

**真实取证(本机,不入库)**

- Bug B 浅+深 400% 前后对比完成并量化:浅色主题修复前墨迹 25 分位亮度 130、仅 2.1% 像素达深墨(Effect 位图把笔画洗成中灰),修复后 P25=29(全对比度)、57.5% 深墨;深色 72.0%→88.8%。**ClearType 彩色镶边未出现**:整窗 Mica alpha 表面令全部文本维持灰度抗锯齿(规划已列「次因」;2a 恢复的是对比度与像素对齐,彻底恢复 RGB ClearType 需不透明窗口表面,属另一权衡)——此观察留档。
- Bug A 真实拖拽录屏多次尝试(85s@30fps、20s@60fps、16s@60fps)均未捕捉到有效拖拽:本机用户全天在场,注入鼠标位置与其真实输入竞速失败(拖拽启动即被抢占);CUA 特权注入通道本会话已停用且不可重启;注入键盘不触发 RegisterHotKey(Windows 对注入输入的过滤,已实验证实)。以 STA 闪烁/跳变复现测试(修复前红、修复后绿)为机械证据;四方向拖动录屏待机器空闲时补录。

**CI、发行与装机**

- PR #57/#58/#59/#60 全部三端全绿合入 main(a04bfe9);main 门运行 36102357141 全绿,统一发行 [v1.11.3](https://github.com/Oiawlm/floating-transfer-station/releases/tag/v1.11.3) 产物取自该运行:`FloatingTransferStation-Setup-1.11.3.exe`(SHA256 `5e3f9a21d91a6c381351e762ee08b09da88a8a2edadac230ce68ded4d3ca36ac`)、`FloatingTransferStation-1.11.3-osx-arm64.zip`(`16a4859f7893fbe8368d7fe0f374f16137e77f0ca0203be6a10e92d9ff2270d8`,ad-hoc 签名未公证、原生冒烟通过)、`SHA256SUMS.txt`、Mac 验证元数据 JSON。`build-release.ps1 -ForRelease` 本地通过。
- 本机:优雅关闭运行实例 → 静默原地更新 1.11.2→1.11.3+a04bfe9(退出码 0)→ 重启验证版本 1.11.3.0 与 HKCU Run 自启值正常,已恢复用户实例。

**委派情况**:本窗口未外包(wbx 适用于独立模块草稿;本次两修复均在冻结相邻的交互层,自研)。

**失误与补救(如实)**:①一次取证辅助脚本以 `D:\App\*` 过滤进程过宽,向同目录下无关应用(微信/飞书/豆包等)窗口误发了 WM_CLOSE(多收起到托盘、无数据损坏;当时 50+ 进程仍存活);已改精确路径过滤并向用户说明。②误杀了用户运行实例一次(PID 误判),取证完成后已原样恢复。③PR #60 契约疏漏一轮(上述)。

**待裁决清单(请用户过目)**

1. Bug A 真实四方向拖动录屏未取得(环境受阻,详见上);建议机器空闲时(如夜间)以隔离实例补录,或用户手动拖一次录屏供归档。
2. Mica alpha 表面令全窗文本灰度抗锯齿(即使 2a 后):若用户希望恢复 RGB ClearType 彩色渲染,需评估放弃整窗 alpha(如材质回退不透明壳)或按元素绘制到不透明位图——涉及视觉权衡,建议作为候选池方向裁决。
3. `LiftAnimation` 悬停 -1 DIP 在 125%/150% DPI 下的瞬态亚像素模糊(规划列为瞬态项不强修):观察后可决定是否按整 DIP 取整。

## 2026-09-25(夜间接力第二晚,切片 1:Bug A 拖拽录屏补录,纯取证不发版)

**方向与决策**:执行 1.11.3 日间条目待裁决 #1 的建议——"机器空闲时(如夜间)以隔离实例补录"。夜间用户睡眠、无输入竞速,注入拖拽可完整保持。

**取证过程**

- 隔离实例:已装 1.12.0 安装版 + `FTS_PREVIEW_DATA_DIR=D:ts-demo-night`(仓库外中性路径),种子 7 条文本卡(置顶 2 + 普通 5);优雅关闭滞留的真实实例后强制结束(恢复后重启,版本 1.12.0 验证通过,真实数据目录未被预览实例触碰(现有 6 条内容为用户白天自行整理的结果,与取证前一致))。
- 工具链:DPI 感知 PowerShell 注入(SetCursorPos/mouse_event,物理像素)+ ffmpeg gdigrab 30fps 录制展开窗口区(626×960,右缘裁切越界 12px 已钳制)+ computer-use 无障碍树定位(锁定"保持展开"按钮消除录制期收起风险,OCR/暗带聚类定位 7 张卡片行,间距 96 物理px)。
- 编排:置顶项下向跨区(释放后钳制回置顶区=修复语义)、普通项在置顶边界 ±14px 往复 4 次(原闪烁区)、普通项上向进置顶区下方、末项下向空白区、长距上拖。
- 首轮失败一次:libx264 拒奇数宽(627)——重置种子重录为 626 宽。

**证据(不入库)**:`artifacts/bug-a-recapture/bug-a-indicator-stability.mp4`(42.0s,269KB,30fps)。机械佐证:种子板终态顺序变化证明拖拽真实生效(三上移过二;跨区释放后置顶项仍回置顶区);t=4.0s 帧检测到指示条强调色横线于置顶区底部钳制槽(y266-268),全程列表 8 带完整、无收起无异常。

**结论与待办**:1.11.3 待裁决 #1 的"真实四方向拖动录屏"已取得,请用户观看 mp4 确认无闪烁观感后关闭该条;观感确认属于人眼判断,机械证据(指示条位置连续、无跨区隐藏-重现)已在帧级成立。本切片零代码改动,不发版(版本保持 1.12.0)。

## 2026-09-25(夜间接力第二晚,切片 2:内容搜索切片 1,1.13.0)

**方向与决策**:执行候选池共识方向「内容搜索与筛选」的设计草案切片 1(当前分类只读过滤)。首夜 04:00 曾以"冻结相邻 UI 风险"搁置;本晚为新鲜会话+整夜预算,按设计草案实施。

**实现**

- Core `MainWindowViewModel` 增搜索态(Enter/Exit/SearchText/SearchTypeFilter,退出清条件);WPF 新增 `MainWindow.Search.cs` 与头部搜索 UI(放大镜按钮位于保持展开开关与齿轮之间、搜索态条:输入框+图片/文本 chip+清空+退出,占位提示「搜索当前分类」)。
- 过滤作用于 `BoardList` 集合视图 Filter 谓词(只读派生视图):关键词大小写不敏感子串匹配文字内容,类型 chip 与关键词「与」关系(都不选/都选=全部);结果保持先置顶后普通顺序;空态居中「无匹配结果」+一键清空。
- 键位:Ctrl+F 进入(与 F2 同构门),Esc/关闭/切类退出;复盘标签不参与(冻结)。搜索态收起抑制并入 `IsPanelEditHoldActive()`(与文本编辑保持同构);禁用拖拽(内部排序落点按过滤视图计算会得到错误源索引——设计草案风险 1 的落实)与 Ctrl+A;进入清空选择。
- **测试先行发现真缺陷**:退出搜索时隐藏输入框仍持逻辑焦点,持续抑制收起——退出时焦点交还列表修复。

**委派(wbx fanout,ai lane 1 次成功)**:CHANGELOG/README 文案草稿交叉核对,与自写版本一致,采用自写(含索引风险与 Mac 说明)。

**测试与质量门**:窗口级 6 项 + Core VM 3 项;齿轮位置断言按新布局更新;全量 885 项,四连过。**环境教训**:全量首跑 4 失败中 3 处为切片 1 真实拖拽把物理光标留在窗口区域所致(IsMouseOver=true 抑制收起计时器),移开光标即绿——记录为取证副作用;第 4 处为齿轮位置契约更新。

**CI、发行与装机**:PR #65 首轮 CI 全绿,合并 main(197d625),main 门 36157083010 全绿,统一发行 [v1.13.0](https://github.com/Oiawlm/floating-transfer-station/releases/tag/v1.13.0)。本机安装:首次退出码 1(重试即成功,与既往观察到的安装器首跑不稳定一致,建议候选池立安装器可靠性项);重启后 1.13.0.0(PID 28216),自启与登记完好。首次提交曾误纳入白天 README 会话的未跟踪资产,撤回重提(资产保持未跟踪)。

## 2026-09-25(夜间接力第二晚,切片 3:卡片内容就地编辑,1.14.0)

**方向与决策**:用户 2026-09-25 白天补充澄清候选池方向「分类标签内容直接编辑=全部卡片内容可编辑(后续 Markdown)」,据此实施切片 1(文字卡就地编辑)。

**实现**

- 双击文字卡在原位打开覆盖式编辑器(`CardTextEditorHost` 定位到卡片容器,不动卡片模板与虚拟化);Enter 保存、Esc 取消、失焦保存;空白视为取消(与插件「不得清空」精神一致,提示后不改内容)。
- `BoardMutationService.UpdateItemTextAsync`:门内串行 + 原子持久化,失败还原原文本;`BoardItem.Text` 从 init-only 改为可设置(实例身份/顺序/置顶/选择引用全部保持);`BoardService.UpdateText/FindItem`。
- 编辑期间收起抑制复用既有键盘焦点保持原因(编辑器在窗口内,`IsPanelTextEditor` 自动覆盖,零新增状态机);图片卡/改名进行中/搜索过滤期不进入编辑。

**测试与质量门**:窗口级 5 项 + 服务级 1 项;全量 891 项,四连过。测试工程注记:MouseDoubleClick 是直接路由事件,测试需提升到挂接方(BoardList)而非容器;保存失败用例须在关闭前放开 FailSave(窗口关闭序列同样走保存,这是产品语义)。

**CI、发行与装机**:PR #66 首轮 CI 全绿,合并 main(b22f563),main 门 36159793131 全绿,统一发行 [v1.14.0](https://github.com/Oiawlm/floating-transfer-station/releases/tag/v1.14.0)。本机首装再次退出码 1、重试即成功——**连续两版复现**,已立候选池项(见下);重启后 1.14.0.0(PID 52632),自启完好。
