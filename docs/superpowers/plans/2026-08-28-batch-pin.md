# 批量置顶与批量取消置顶实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为当前分类的多选内容增加一次性批量置顶/取消置顶入口，并保持现有顺序、选择、滚动与保存回滚契约。

**Architecture:** UI 在选择变化时计算单一目标状态，并把按显示顺序捕获的 ID 交给现有 `BoardMutationService.SetPinnedAsync`。数据层已有批量排序和原子回滚能力，本次只增加标题栏入口、窗口编排测试和受影响文档，不改变持久化或拖放契约。

**Tech Stack:** .NET 10、WPF、C#、MSTest、GitHub Actions

---

### Task 1: 锁定批量按钮的状态契约

**Files:**
- Modify: `tests/FloatingTransferStation.Tests/MainWindowInteractionTests.cs`

- [ ] **Step 1: 写空选择、混合选择和全置顶选择的失败测试**

在现有标题栏测试附近增加 `BatchPinButton_TracksSelectionAndAdvertisesOneDeterministicAction`。测试创建一个普通项和一个置顶项，取得 `BatchPinButton`，并依次断言：空选择时折叠；选择混合项时可见、工具提示与自动化名称均为 `置顶已选 2 项`；只保留置顶项时两者均为 `取消置顶已选 1 项`。

```csharp
var batchPin = (Button?)window.FindName("BatchPinButton");
Assert.IsNotNull(batchPin);
Assert.AreEqual(Visibility.Collapsed, batchPin.Visibility);

list.SelectedItems.Add(pinned);
list.SelectedItems.Add(normal);
CompleteLayout(window);
Assert.AreEqual(Visibility.Visible, batchPin.Visibility);
Assert.AreEqual("置顶已选 2 项", batchPin.ToolTip);
Assert.AreEqual("置顶已选 2 项", AutomationProperties.GetName(batchPin));

list.SelectedItems.Remove(normal);
CompleteLayout(window);
Assert.AreEqual("取消置顶已选 1 项", batchPin.ToolTip);
Assert.AreEqual("取消置顶已选 1 项", AutomationProperties.GetName(batchPin));
```

- [ ] **Step 2: 运行目标测试并确认红灯原因是按钮不存在**

Run:

```powershell
& 'D:\Download\Agent Vault\悬浮中转站\.tools\dotnet\dotnet.exe' test tests\FloatingTransferStation.Tests\FloatingTransferStation.Tests.csproj -c Release --no-restore --filter "FullyQualifiedName~BatchPinButton_TracksSelectionAndAdvertisesOneDeterministicAction"
```

Expected: FAIL，`window.FindName("BatchPinButton")` 返回 `null`。

- [ ] **Step 3: 提交红灯测试**

```powershell
git add tests\FloatingTransferStation.Tests\MainWindowInteractionTests.cs
git commit -m "test: define batch pin selection states"
```

### Task 2: 锁定批量执行、慢保存与回滚契约

**Files:**
- Modify: `tests/FloatingTransferStation.Tests/MainWindowInteractionTests.cs`

- [ ] **Step 1: 写混合选择一次置顶的失败测试**

增加 `BatchPinButton_MixedSelectionPinsAllInSourceOrderAndPreservesSelectionAndScroll`：选择一个既有置顶项与两个普通项，点击按钮，等待保存完成，断言三个选择项均置顶、普通项按点击前的列表顺序位于置顶区顶部、未选项不变、只保存一次、选择集合和滚动偏移保持。

- [ ] **Step 2: 写全置顶选择一次取消置顶的失败测试**

增加 `BatchPinButton_AllPinnedSelectionUnpinsAsOneSourceOrderedBlock`：选中两个置顶项，点击按钮后断言二者成为普通项并以原源顺序出现在普通区顶部，现有未选置顶项仍位于置顶区。

- [ ] **Step 3: 写慢保存期间状态稳定的失败测试**

使用 `BlockingFirstSuccessfulSaveBoardStore` 增加 `BatchPinButton_SlowSaveKeepsSelectionAndDeleteScope`，在首次保存阻塞期间断言选择集合、`SelectedCountBadge`、删除按钮工具提示和批量按钮工具提示仍与两项选择一致；释放后等待完成并再次断言。

- [ ] **Step 4: 写保存失败精确回滚的失败测试**

使用 `RecordingBoardStore.SaveFailure` 增加 `BatchPinButton_SaveFailureRestoresStateOrderSelectionAndScroll`，断言对象顺序、每项置顶状态、选择集合、滚动偏移和 `置顶状态未保存，内容已恢复。` 全部恢复。

- [ ] **Step 5: 运行全部批量按钮测试并确认红灯来自按钮缺失**

```powershell
& 'D:\Download\Agent Vault\悬浮中转站\.tools\dotnet\dotnet.exe' test tests\FloatingTransferStation.Tests\FloatingTransferStation.Tests.csproj -c Release --no-restore --filter "FullyQualifiedName~BatchPinButton_"
```

Expected: FAIL，所有新增测试均在查找 `BatchPinButton` 或触发其行为前失败，而非测试初始化错误。

- [ ] **Step 6: 提交完整红灯测试集**

```powershell
git add tests\FloatingTransferStation.Tests\MainWindowInteractionTests.cs
git commit -m "test: define batch pin interaction recovery"
```

### Task 3: 实现最小标题栏入口和批量操作

**Files:**
- Modify: `src/FloatingTransferStation/Views/MainWindow.xaml`
- Modify: `src/FloatingTransferStation/Views/MainWindow.SelectionAndDeletion.cs`

- [ ] **Step 1: 在删除按钮前增加默认折叠的批量图钉按钮**

```xml
<Button x:Name="BatchPinButton"
        Style="{StaticResource HeaderActionButtonStyle}"
        ToolTip="置顶已选内容"
        AutomationProperties.Name="置顶已选内容"
        Visibility="Collapsed"
        Click="BatchPinButton_Click">
  <Viewbox Width="16" Height="16">
    <Path Data="M8,3 L16,3 L14,8 L17,11 L13,11 L12,20 L10,11 L6,11 L9,8 Z"
          Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"
          Stretch="Uniform" />
  </Viewbox>
</Button>
```

- [ ] **Step 2: 让选择变化同步按钮状态**

在 `BoardList_SelectionChanged` 末尾调用以下方法：

```csharp
private void UpdateBatchPinButton()
{
    var selected = BoardList.SelectedItems.OfType<BoardItem>().ToArray();
    BatchPinButton.Visibility = selected.Length > 0
        ? Visibility.Visible
        : Visibility.Collapsed;
    if (selected.Length == 0)
    {
        return;
    }

    var action = selected.All(item => item.IsPinned) ? "取消置顶" : "置顶";
    var label = $"{action}已选 {selected.Length} 项";
    BatchPinButton.ToolTip = label;
    AutomationProperties.SetName(BatchPinButton, label);
}
```

- [ ] **Step 3: 把点击编排到现有原子批量服务**

```csharp
private async void BatchPinButton_Click(object sender, RoutedEventArgs e)
{
    e.Handled = true;
    if (_isClosing || _viewModel.ActivePanel is not { } activePanel)
    {
        return;
    }

    var selectedBefore = CaptureSelectedItemIds();
    if (selectedBefore.Length == 0)
    {
        return;
    }

    var selectedIds = activePanel.Items
        .Where(item => selectedBefore.Contains(item.Id))
        .Select(item => item.Id)
        .ToArray();
    if (selectedIds.Length != selectedBefore.Length)
    {
        return;
    }

    var targetPinned = activePanel.Items
        .Where(item => selectedBefore.Contains(item.Id))
        .Any(item => !item.IsPinned);
    var category = activePanel.Category;
    var offset = CurrentScrollOffset();
    await _mutations.SetPinnedAsync(selectedIds, targetPinned);
    await Dispatcher.InvokeAsync(
        () =>
        {
            if (_viewModel.ActivePanel?.Category == category)
            {
                RestoreSelection(selectedBefore);
                RestoreExplicitScrollOffset(category, offset);
            }
        },
        DispatcherPriority.Send);
}
```

- [ ] **Step 4: 运行批量与单卡图钉测试并确认绿灯**

```powershell
& 'D:\Download\Agent Vault\悬浮中转站\.tools\dotnet\dotnet.exe' test tests\FloatingTransferStation.Tests\FloatingTransferStation.Tests.csproj -c Release --no-restore --filter "FullyQualifiedName~BatchPinButton_|FullyQualifiedName~PinButton_"
```

Expected: 新增批量测试和既有单卡图钉测试全部 PASS。

- [ ] **Step 5: 提交最小实现**

```powershell
git add src\FloatingTransferStation\Views\MainWindow.xaml src\FloatingTransferStation\Views\MainWindow.SelectionAndDeletion.cs
git commit -m "feat: add batch pin header action"
```

### Task 4: 同步公开文档与项目指南

**Files:**
- Create: `PROJECT_GUIDE.md`
- Modify: `README.md`
- Modify: `ROADMAP.md`
- Modify: `CHANGELOG.md`
- Modify: `tests/FloatingTransferStation.Tests/LifecycleTests.cs`

- [ ] **Step 1: 先修改生命周期测试锁定文档结果**

把仍要求 README/路线图包含“批量置顶尚未实现”的断言替换为：README 包含 `批量置顶或取消置顶` 的操作说明且不再包含旧限制；ROADMAP 不再把批量置顶列为下一步；CHANGELOG 的 `未发布` 区包含该能力。

- [ ] **Step 2: 运行生命周期测试并确认红灯来自旧文档**

```powershell
& 'D:\Download\Agent Vault\悬浮中转站\.tools\dotnet\dotnet.exe' test tests\FloatingTransferStation.Tests\FloatingTransferStation.Tests.csproj -c Release --no-restore --filter "FullyQualifiedName~PublicRepositoryDocuments"
```

Expected: FAIL，断言指出 README、ROADMAP 或 CHANGELOG 尚未同步。

- [ ] **Step 3: 更新 README、ROADMAP 与 CHANGELOG**

- README 常用操作增加“多选后点击顶部图钉可统一置顶；全为置顶时统一取消置顶”，删除当前限制和下一步中的旧描述。
- ROADMAP 把“已确定的下一步”更新为仍待进入开发的后续研究项，不再声称批量置顶未实现。
- CHANGELOG 增加 `## 未发布`，记录批量置顶/取消置顶及其原子保存语义。

- [ ] **Step 4: 创建简洁的 PROJECT_GUIDE.md**

记录项目类型、主要目录、真实的 restore/format/test/build/release 命令、WPF STA 测试注意点、不可提交目录，以及持久化、置顶分区、拖放和卸载边界，供后续代理从仓库恢复事实。

- [ ] **Step 5: 复跑生命周期测试并确认绿灯**

Run: Task 4 Step 2 的同一命令。

Expected: PASS。

- [ ] **Step 6: 提交文档同步**

```powershell
git add PROJECT_GUIDE.md README.md ROADMAP.md CHANGELOG.md tests\FloatingTransferStation.Tests\LifecycleTests.cs
git commit -m "docs: document batch pinning workflow"
```

### Task 5: 完整验证、真实 UI 证据与 GitHub 交付

**Files:**
- No tracked file required for checks
- Create locally only: `artifacts/batch-pin-verification.png`

- [ ] **Step 1: 运行格式验证**

```powershell
& 'D:\Download\Agent Vault\悬浮中转站\.tools\dotnet\dotnet.exe' format FloatingTransferStation.slnx --verify-no-changes --no-restore
```

Expected: exit code 0，无格式差异。

- [ ] **Step 2: 运行 Release 全量测试**

```powershell
& 'D:\Download\Agent Vault\悬浮中转站\.tools\dotnet\dotnet.exe' test FloatingTransferStation.slnx -c Release --no-restore
```

Expected: 所有测试 PASS，0 failed。

- [ ] **Step 3: 运行严格 Release 构建**

```powershell
& 'D:\Download\Agent Vault\悬浮中转站\.tools\dotnet\dotnet.exe' build FloatingTransferStation.slnx -c Release --no-restore -warnaserror
```

Expected: 0 warnings, 0 errors。

- [ ] **Step 4: 启动真实 WPF 应用并现场验证**

从 Release 输出启动 `悬浮中转站.exe`，创建或使用隔离测试数据，实际多选混合卡片完成批量置顶，再对全置顶选择完成批量取消置顶；确认标题栏工具提示、选择反馈和顺序。保存截图到 `artifacts/batch-pin-verification.png`，确认 `git status` 不包含该文件。

- [ ] **Step 5: 检查差异与仓库卫生**

```powershell
git diff origin/main...HEAD --check
git status --short
git log --oneline origin/main..HEAD
```

Expected: 无 whitespace error、无未提交跟踪文件、提交范围只覆盖规范、计划、功能、测试和文档。

- [ ] **Step 6: 推送、创建 PR、等待 CI 并合并**

```powershell
git push -u origin HEAD
gh pr create --base main --title "feat: add batch pinning" --body "## Summary`n- add a deterministic batch pin action for the current selection`n- preserve ordering, selection, scroll position, and atomic rollback`n- update public docs and project guidance`n`n## Verification`n- Release tests`n- strict Release build`n- real WPF interaction screenshot`n`nCloses #3"
gh pr checks --watch
gh pr merge --squash --delete-branch
```

PR 正文总结批量入口、混合选择语义、回滚保证和本地验证，并包含 `Closes #3`。Expected: CI 全绿、PR 合并、Issue #3 关闭。

- [ ] **Step 7: 刷新本地主仓库并核对远端**

```powershell
git -C 'D:\Download\Agent Vault\悬浮中转站' pull --ff-only origin main
gh issue view 3 --json state,url
git -C 'D:\Download\Agent Vault\悬浮中转站' status --short --branch
```

Expected: 主仓库与 `origin/main` 同步、Issue 状态为 CLOSED、工作树干净。
