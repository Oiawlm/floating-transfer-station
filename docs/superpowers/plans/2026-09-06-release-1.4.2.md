# 1.4.2 Release Implementation Plan

> **For agentic workers:** Use superpowers:subagent-driven-development for the bounded metadata preparation and independent reviews. The user authorized completing the next delivery step on 2026-09-06; continue through publication without another approval pause.

**Goal:** 发布已合并维护修复为 1.4.2，并交付从最终主线提交构建、上传及下载回验一致的 Windows 安装包。

**Architecture:** 保留 `03b748b` 的产品行为和持久化格式，只同步版本与发布资料。发布准备通过 `codex/release-1.4.2` 的普通 PR 进入 main；最终轻量 tag 和安装包均绑定 squash 后的主线提交，不发布分支候选包。

**Tech Stack:** .NET 10、WPF、MSTest、PowerShell、Inno Setup、GitHub Actions/CLI。

## Task 1: 发布契约与元数据

**Files:** `LifecycleTests.ReleaseMetadata.cs`、`LifecycleTests.Installer.cs`、项目 `.csproj`、`ProductIdentity.cs`、`installer/悬浮中转站.iss`、README、CHANGELOG、PROJECT_GUIDE、`docs/README.md`。

- [x] 先把测试中的当前版本常量、安装器字面量、唯一下载资产、README 和 PROJECT_GUIDE 期望改为 1.4.2。保留 1.4.1 及更早历史版本断言；增加新版本章节中的四项维护修复断言。

```csharp
const string expectedVersion = "1.4.2";
// Public materials must contain:
// FloatingTransferStation-Setup-1.4.2.exe
// 1.4.2 已通过自动质量门和安装包构建验证
// 不属于 1.4.2 承诺
// 当前稳定发布为 1.4.2
StringAssert.Contains(changelogSections["1.4.2"], "保留同级用户文件和程序文件");
StringAssert.Contains(changelogSections["1.4.2"], "保留新分类的选择与删除操作提示");
StringAssert.Contains(changelogSections["1.4.2"], "仍保持内容处理顺序");
StringAssert.Contains(changelogSections["1.4.2"], "开发启动不再覆盖正式安装的自启路径");
```

- [x] 运行三项当前版本/公开资料/安装器启动契约，确认版本不匹配的 RED；再同步三处生产版本为 `1.4.2`，将未发布记录完整移至新 `## 1.4.2` 段落，复跑同一选集为 GREEN。

```powershell
& .\.tools\dotnet\dotnet.exe test FloatingTransferStation.slnx -c Release --no-restore --filter 'FullyQualifiedName~ReleaseMetadata_UsesOneConsistentVersion|FullyQualifiedName~PublicReleaseMaterials_DescribeInstallerLicenseRoadmapAndContribution|FullyQualifiedName~Installer_ForceClosesRunningAppBeforeMutexFallbackAndAlwaysLaunchesInteractiveReplacement'
```

- [x] `docs/README.md` 将维护记录定位到 `03b748b` / 1.4.2，加入本发布计划链接；历史计划及通用发布检查测试的示例版本不改写。

## Task 2: 质量门与候选验证

- [ ] 独立规格与质量审查确认只同步发布契约，不削弱 CI 必需名、安装清理或数据边界。
- [x] 在工作树运行格式验证、完整正式发布构建（包含 Release 全量测试、隔离清理和安装包编译）、严格构建；记录所有退出码和截图。预期 395/395，无跳过，严格构建零警告零错误。

```powershell
& .\.tools\dotnet\dotnet.exe format FloatingTransferStation.slnx --verify-no-changes --no-restore
& .\scripts\build-release.ps1 -ForRelease
& .\.tools\dotnet\dotnet.exe build FloatingTransferStation.slnx -c Release --no-restore -warnaserror
git diff --check
```

- [ ] 在 `scripts/test-installed-lifecycle.ps1` 补充真实包安装、同版本原地重装及卸载验证，并由 `.github/workflows/ci.yml` 的既有质量任务执行、保存证据。脚本必须同时要求 Windows、`GITHUB_ACTIONS=true`、`RUNNER_ENVIRONMENT=github-hosted`；拒绝既有产品注册/进程/互斥/默认数据及目录链接。只预登记 RUNNER_TEMP 唯一路径内的合成 Data，程序与 Data 同级，验证登记值和内容哈希，最后卸载并确认无关哨兵保留。每个子进程最多等待 60 秒，失败保留日志；本地只运行语法检查与环境拒绝验证。没有数据目录静默参数，明确不声称验证跨目录迁移或登录自启；迁移清理继续使用现有原生隔离与调用方契约。
- [x] 核对候选 FileVersion=1.4.2、非空包、签名状态和 SHA256；候选不上传为正式资产。

## Task 3: 提交、PR 与普通合并

- [ ] 精确暂存 Task 1/2 的版本、测试、发布资料、CI 工作流、新安装态脚本与本计划，完成 `release: prepare 1.4.2` 提交并普通推送 `origin codex/release-1.4.2`。
- [ ] 使用准备好的正文文件创建 PR，等待该提交的“格式、测试与构建”必需检查成功；用 `gh pr merge --squash --match-head-commit` 合并，不使用 admin 或改保护规则。
- [ ] 获取远端，在主工作目录 `main` 快进到合并提交，确认合并树与候选树一致；等待该 main 提交质量门成功。

## Task 4: 最终安装包与 Release

- [ ] 在干净最终 main 上再次运行 `build-release.ps1 -ForRelease`。确认发布应用 ProductVersion 为 `1.4.2+最终提交`，安装包 FileVersion=1.4.2；复制为 `artifacts/release/FloatingTransferStation-Setup-1.4.2.exe` 并比较哈希。
- [ ] 检查本地/远端尚无 v1.4.2。创建轻量 tag 指向最终主线，普通推送；遇不确定结果先查询实际远端状态再重试。
- [ ] 发布说明包含四项可靠性修复、Windows 10/11 64 位和正确安装文件名。使用 `gh release create v1.4.2 --verify-tag --latest --notes-file` 上传唯一英文名安装包，不覆盖旧 Release。
- [ ] 用新建的唯一目录下载 v1.4.2 资产，断言本地上传文件、GitHub digest 和下载文件 SHA256/长度一致；核对 tag/main 提交、非草稿非预发布和 Latest 状态。
- [ ] 在自动化记录与忽略的发布证据中写入实际结果、提交、CI run 和哈希。未完成事项准确保留，历史工作树不删除。

## 状态

2026-09-06：从已通过质量门的 `03b748b` 建立独立发布工作树。本计划步骤只在取得实际证据后完成；最终交付状态以 PR、tag、Release 和本轮记录为准。

- 本地发布准备：版本契约 3 项 RED → 3/3 GREEN；完整 395/395、跳过 0；格式与严格构建通过，0 警告/错误；`-ForRelease` 记录检查、6 个原生清理案例/23 断言及安装包编译通过。
- 候选包 FileVersion=1.4.2，44,398,779 bytes，签名状态 NotSigned，与既有发布方式相同；仅作工作树候选证据。真实 WPF 截图已查看。
- 安装态脚本静态语法通过，本地调用在任何注册表/进程/数据操作前按预期拒绝；真实正向执行等待发布 PR 的一次性 Windows CI。独立规格审查通过，质量审查与后续发布记录以 PR 和本轮自动化记录为准。
