# 悬浮中转站贡献工作规则

- 修改前先读 `README.md`、`CONTRIBUTING.md` 和直接相关代码/测试。
- 修复先复现；功能先确认用户结果和公开契约。不要用大重写代替根因分析。
- 保留持久化原子性、置顶分区、批量顺序、拖放源数据和安全卸载边界。
- 修改代码后运行相关测试；准备提交前运行格式验证、Release 全量测试和严格构建。
- UI 或交互变化必须提供真实截图或录屏，并更新受影响的 README、路线图或更新记录。
- 不提交 `.tools/`、`artifacts/`、`TestResults/`、用户内容、凭据或本机截图。

## 双平台维护与 GitHub 持续授权

- 用户于 2026-09-08 明确要求：以后每次维护 Windows 端，同步完成 Mac 端适用的实现、文档和验证；只影响单一系统时说明原因，并确认另一端没有回归。两端共用业务核心和 `version.txt`。
- 用户于 2026-09-08 进一步明确：1.6.0 之后的 Windows 端以 Windows 11 为开发与验证目标，不再针对 Windows 10 单独做兼容适配、维护或测试，也不将 Windows 10 兼容性作为发布门槛。下一次准备版本时同步更新公开支持说明；本次仅记录后续约定，现有 1.6.0 安装包、下载入口和发行记录保持不变。
- 用户于 2026-09-08 进一步明确：1.6.0 之后的 Mac 端只考虑 Apple Silicon（M 系列芯片，`osx-arm64`），不再承担 Intel Mac 的适配、维护、验证或新包发布。下次开发或准备版本时同步收敛 CI、默认构建目标与下载说明；本次仅记录后续约定，已发布的 1.6.0 Intel 包及其校验文件、元数据和下载记录全部保留，不撤回、重打或重发。
- 按依赖选择顺序：共享契约先确定，独立的平台适配可交给子代理并行；有先后依赖则顺序完成。由主代理统一集成和检查，不为并行而复制业务规则或制造冲突。
- 用户持续授权本仓库范围内的工作在完成适用验证后，创建任务分支、Git 提交并普通推送到既定 GitHub 仓库 `Oiawlm/floating-transfer-station`，无需每次再次确认；需要 PR 才能触发质量门时可创建 PR，并继续处理本次改动导致的 CI 失败。
- 用户进一步明确版本交付目标：两端代码应进入 main，后续 Windows 安装包和 Apple Silicon Mac 包应放在同一个 GitHub Release，首页按平台并列提供下载；1.6.0 已发布的 Intel Mac 包作为历史产物保留。功能分支、PR 和单独 Mac 预发布是中间状态，不能作为统一版本交付完成的替代。
- 本次合并与统一发布已经获得用户授权；按用户已确认的版本交付目标，在质量门通过后继续完成合并、main 验证和统一发行，不再将“仅代码推送”当作“版本已发布”。Mac 未公证时如实标注测试状态，不因统一发行而虚称已获 Apple 认证。
- 这项授权不包含强制推送、购买、签名凭据配置或其他仓库/全局设置变更。上述事项需要覆盖其具体影响的额外授权。
